#import "utils.typ": *

= 用語集 <sec-appendix-glossary>

本書に出てくる用語を、専門知識がなくても分かるようにまとめました。
分からない言葉が出てきたら、ここを引いてください。

== システムに関する用語 <subsec-glo-system>

#figure(
  stable(
    columns: (auto, 1fr),
    [*用語*], [*意味*],

    [ROS 2],
    [ロボット用のソフトウェアを作るための土台。
     多数の小さなプログラムが、データをやりとりしながら協調して動く仕組みを提供します],

    [ノード],
    [ROS 2 上で動く#tsuyo[プログラム 1 つ]のこと。
     「センサから読む係」「経路を計算する係」のように役割ごとに分かれています],

    [トピック],
    [ノード同士がデータを流すための#tsuyo[通り道]。
     `/fix` のように名前が付いており、「誰かが流し、誰かが受け取る」形で使われます],

    [パッケージ],
    [ノードや設定ファイルをひとまとめにした単位。
     本システムは複数のパッケージの集まりでできています],

    [リポジトリ],
    [ソースコードの保管場所。本システムでは GitHub 上に置かれています],

    [サブモジュール],
    [あるリポジトリの中に、別のリポジトリを組み込む仕組み。
     `--recursive` を付けてクローンすると、中身も一緒に取得されます],

    [ビルド],
    [ソースコードを、実際に動く形に変換する作業。`colcon build` などで行います],

    [Launch ファイル],
    [複数のノードをまとめて起動するための手順書],

    [Nav2],
    [ROS 2 標準の自律走行ソフトウェア。
     地図と現在位置から進む道筋を計算し、障害物を避けながら走ります],
  ),
  caption: [システムに関する用語],
) <tab-glo-system>

== センサと位置に関する用語 <subsec-glo-sensor>

#figure(
  stable(
    columns: (auto, 1fr),
    [*用語*], [*意味*],

    [RSF（RSF-X001）],
    [本システムが対象とする北陽電機製のセンサ。
     3D LiDAR と RTK-GNSS が 1 つの筐体に入っています],

    [3D LiDAR],
    [レーザーで周囲までの距離を測る装置。
     周囲の形を#tsuyo[点の集まり]として取得します],

    [点群（PointCloud）],
    [3D LiDAR が測った#tsuyo[点の集まり]。
     これを貯めていくと立体的な地図になります],

    [IMU],
    [傾きや回転の速さを測る装置。
     どちらが下（重力の向き）かを知るのに使います],

    [GNSS],
    [人工衛星を使って地球上の位置を知る仕組み。GPS もこの一種です],

    [RTK-GNSS],
    [基準局からの補正情報を使い、
     GNSS の精度を数センチまで高める方式],

    [共分散],
    [測定値の#tsuyo[ばらつきの大きさ]を表す数値。
     小さいほど信頼できます。GNSS の精度判定に使われます],

    [fix 率],
    [記録した GNSS データのうち、
     設定した精度を満たしたデータの割合 [%]],

    [オドメトリ],
    [「スタート地点からどれだけ動いたか」を積み重ねて求めた位置。
     積み重ねるため、進むほど誤差が溜まります],

    [LIO],
    [LiDAR Inertial Odometry の略。
     3D LiDAR と IMU を組み合わせて求めるオドメトリ],

    [自己位置推定（Localization）],
    [あらかじめ作った地図と、いま見えている景色を照らし合わせて、
     #tsuyo[地図のどこにいるか]を求めること],
  ),
  caption: [センサと位置に関する用語],
) <tab-glo-sensor>

== 地図と経路に関する用語 <subsec-glo-map>

#figure(
  stable(
    columns: (auto, 1fr),
    [*用語*], [*意味*],

    [SLAM],
    [地図を作りながら、同時に自分の位置も求める技術の総称],

    [3D 点群地図（`.pcd`）],
    [点の集まりでできた立体的な地図。
     自己位置推定に使われます],

    [2D 占有格子地図（`.pgm` / `.yaml`）],
    [平面を細かいマス目に区切り、
     各マスが「通れる／通れない」を表した地図。Nav2 が経路計算に使います。
     `.pgm` が画像、`.yaml` がその縮尺や原点の情報です],

    [占有格子],
    [2D 地図のマス目 1 つ 1 つのこと],

    [解像度],
    [2D 地図のマス目 1 つが実際の何メートルにあたるか。
     `0.05` なら 5 cm 四方],

    [p2o],
    [3D 地図を作る方式の 1 つ。
     位置のつながりを全体で調整（グラフ最適化）し、ゆがみの少ない地図を作ります],

    [lio_raw],
    [3D 地図を作る方式の 1 つ。
     LIO の軌跡に沿って点群を並べるだけの単純な方式。
     長距離では誤差が溜まります],

    [pcd2pgm],
    [3D 点群地図を 2D 占有格子地図に変換する処理],

    [グラフ最適化],
    [多数の位置の関係をまとめて調整し、
     全体としてつじつまの合う配置を求める計算],

    [waypoint（ウェイポイント／経路点）],
    [ロボットが順番に目指す地点。
     位置・向き・属性・到着判定の許容誤差を持ちます],

    [属性],
    [経路点に設定する動作の指定。
     `normal`（通過）、`stop`（停止）、`slow`（減速）の 3 種類],

    [許容誤差（tolerance）],
    [「ここまで近づいたら到着とみなす」という範囲。
     位置は `xy_tolerance` [m]、向きは `yaw_tolerance` [rad] で指定します],

    [シナリオファイル],
    [マルチマップ走行で、
     どの地図をどの順番で走るかを定義した CSV],

    [rosbag],
    [センサから流れてきたデータを、時刻付きでそのまま記録したもの。
     ROS 2 では#tsuyo[フォルダ]として保存されます],
  ),
  caption: [地図と経路に関する用語],
) <tab-glo-map>

== 座標に関する用語 <subsec-glo-frame>

#figure(
  stable(
    columns: (auto, 1fr),
    [*用語*], [*意味*],

    [座標系（フレーム）],
    [位置を測るときの#tsuyo[基準]。
     「地図の原点から見て」「ロボットから見て」など、基準が違えば同じ場所でも数値が変わります],

    [TF],
    [複数の座標系の関係を管理する ROS 2 の仕組み。
     これがつながっていないと、位置を変換できません],

    [`map`], [地図の原点を基準とする座標系],
    [`odom`], [LIO の位置を平面に落とした座標系。Nav2 が使います],
    [`lio_odom`], [LIO が出す立体的な座標系],
    [`base_link`], [ロボット本体を基準とする座標系],

    [UTM 座標],
    [地球を平面に投影して、メートル単位で位置を表す方式。
     p2o の地図作成で内部的に使われます],

    [クォータニオン],
    [向き（姿勢）を 4 つの数値で表す方法。
     経路ファイルの `orientation` がこれにあたります],

    [Yaw（ヨー角）],
    [真上から見たときの向き。
     Map Viewer では −180〜180 度で入力します],
  ),
  caption: [座標に関する用語],
) <tab-glo-frame>

== 操作画面に関する用語 <subsec-glo-gui>

#figure(
  stable(
    columns: (auto, 1fr),
    [*用語*], [*意味*],

    [GUI],
    [ボタンや画面で操作する仕組み。本システムでは Web ブラウザ上で動きます],

    [メイン画面],
    [各機能への入口となるブラウザ画面。現在のモードもここに表示されます],

    [Map Viewer（3D Viewer）],
    [3D/2D 地図と経路を立体的に表示し、経路を編集する画面],

    [Vizanti],
    [ブラウザ上で ROS 2 のデータを表示・操作するツール。
     rosbag の記録と手動操作に使います],

    [RViz2],
    [ROS 2 標準の表示ツール。自律走行中の状態確認に使います],

    [ギズモ],
    [Map Viewer で経路点を選んだときに出る青い半円。
     ドラッグすると向きを変えられます],

    [端末（ターミナル）],
    [文字でコマンドを打ち込む黒い画面。
     処理の進行状況やエラーがここに表示されます],
  ),
  caption: [操作画面に関する用語],
) <tab-glo-gui>

#pagebreak()
