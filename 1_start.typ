#import "utils.typ": *

= はじめに <sec:start>

`hokuyo_navigation2` は、`ROS 2` の自律走行パッケージ #link("https://docs.nav2.org")[`Nav2`] を活用した
RSF-X001（以下、RSF）専用の屋内屋外対応自律走行ソフトウェアです。

RSF の RTK-GNSS の出力と 3D LiDAR による位置を用いて地図作成と経路設計を行い、
それらを用いた自律走行を可能とします。
また、非技術者でも簡単に操作を行うための GUI を用意しているため、
センサ 1 台で自律走行ロボットの構築が可能となります。

== 本書について <subsec-about>

=== 対象読者

本書は、#tsuyo[ROS 2 の知識がない方が単独で操作できること]を目標に書かれています。
コマンドを入力する場面はありますが、
「この文字をそのまま打ち込む」という形で記載していますので、
意味が分からなくても手順どおりに進められます。

#figure(
  stable(
    columns: (auto, 1fr),
    [*こんな方に*], [*読んでいただきたい章*],
    [初めて導入する], [第2章から順に、すべて],
    [すでに導入済みで、現場で操作する], [第4章〜第10章],
    [エラーが出て困っている], [第11章（トラブルシューティング）],
    [ソフトウェアの中身を知りたい], [第2章、付録A],
  ),
  caption: [読者別の読み進め方],
) <tab-readers>

=== 本書の使い方

作業の流れは 5 つの工程に分かれており、章の順番がそのまま作業の順番になっています。

#v(4pt)
#flow("① データ取得", "② マッピング", "③ 経路設計", "④ 2D地図変換", "⑤ 自律走行")
#v(6pt)

#tip[
  #tsuyo[エラーが出たときは、まず第11章を開いてください。]\
  画面や端末に表示された文字を、第11章の見出しから探すのが最短の道です。
  各エラーには `E-101` のような番号が付いており、
  「症状 → 原因 → 対処 → 確認」の順に記載しています。
]

=== 記号の見方 <subsec-notation>

本書では、内容の性質に応じて次の囲みを使い分けています。

#note[知っておくと理解が深まる補足です。読み飛ばしても操作はできます。]

#tip[作業が楽になる、あるいは失敗しにくくなるコツです。]

#plain[専門用語をかみ砕いた言い換えです。用語が分からないときに読んでください。]

#warn[これを守らないと、作業をやり直すことになる項目です。]

#danger[人や機械が傷つく可能性のある項目です。必ずお読みください。]

また、文中では次の表記を使います。

#figure(
  stable(
    columns: (auto, 1fr),
    [*表記*], [*意味*],
    [#btn[データ取得]], [ブラウザ画面上のボタン。この名前のボタンをクリックします],
    [#kbd[Esc]], [キーボードのキー。このキーを押します],
    [#path[config/rsf_node_config.yaml]], [ファイルやフォルダの場所],
    [`/fix`], [ROS 2 のトピック名やコマンド、パラメータ名],
  ),
  caption: [本書の表記],
) <tab-notation>

コマンドを入力する場面では、次のような灰色の枠で示します。
枠の中身をそのまま端末に打ち込んでください。

#terminal[```bash
ros2 topic list
```]

画面や端末に#tsuyo[表示される]内容は、次のような黒い枠で示します。
こちらは入力するものではなく、「こう表示されれば正常」という見本です。

#console(title: "端末の表示例")[```
Flask Server starting at http://0.0.0.0:5050
WebSocket Proxy server started at ws://0.0.0.0:5050/ws
```]

== 安全にお使いいただくために <subsec-safety>

#danger[
  本ソフトウェアは#tsuyo[ロボットを自動で動かします]。
  操作を始める前に、必ず次の項目をご確認ください。

  - 非常停止スイッチの位置を確認し、#tsuyo[いつでも押せる場所にオペレータが立つこと]
  - ロボットの進路上と周囲に、人・物・段差・落下の危険がないこと
  - 初めての経路では、#tsuyo[ロボットの横を歩いて付いていける速度]に設定すること
  - 作業エリアに関係者以外が立ち入らないよう、必要に応じて区画を設けること
]

#warn[
  自律走行の開始画面には確認用のチェック欄があり、
  すべてにチェックを入れるまで開始ボタンを押せない仕組みになっています。
  これは操作ミスを防ぐためのものです。
  #tsuyo[形式的にチェックを入れるのではなく、実際に目で確認してからチェックしてください。]
]

#note[
  ソフトウェアによる停止操作（#btn[ロボット停止] など）は、
  反映まで数秒かかることがあります。
  ロボットが人や物に接触しそうな状況では、
  #tsuyo[ソフトウェアの操作を試す前に、必ず物理的な非常停止スイッチを押してください。]
]

== 対応バージョンと動作環境 <subsec-version>

#figure(
  stable(
    columns: (auto, 1fr),
    [*項目*], [*内容*],
    [対象ソフトウェアバージョン], [`hokuyo_navigation2` 1.0.0],
    [OS], [`Ubuntu 22.04 LTS`],
    [ROS 2 ディストリビューション], [`Humble Hawksbill`],
    [対応ブラウザ], [Google Chrome、Microsoft Edge など Chromium 系ブラウザを推奨],
  ),
  caption: [動作環境],
) <tab-environment>

#note[
  GUI は Web ブラウザ上で動作します。
  ロボットに搭載したパソコンで直接操作するほか、
  同じネットワークにつながった別のパソコンやタブレットからも操作できます。
  別の端末から操作する場合の URL については @sec-gui を参照してください。
]

本書は、@tab-doc-scope に示す機能について説明しています。

#figure(
  stable(
    columns: (auto, auto, 1fr),
    [*章*], [*機能*], [*内容*],
    [第2章], [システム構成], [ソフトウェアの全体像とリポジトリ構成],
    [第3章], [セットアップ], [ROS 2 と本ソフトウェアの導入],
    [第4章], [GUI の起動], [サーバの起動・停止と画面の説明],
    [第5章], [コンフィグ設定], [地図作成パラメータとシナリオファイル],
    [第6章], [データ取得], [センサデータ（rosbag）の記録],
    [第7章], [マッピング], [3D 点群地図の作成],
    [第8章], [経路設計], [経路（waypoint）の編集],
    [第9章], [2D 地図への変換], [Nav2 用 2D 地図の生成],
    [第10章], [自律走行], [単一マップ走行とマルチマップ走行],
    [第11章], [トラブルシューティング], [エラーの原因と対処],
    [付録A], [パッケージリファレンス], [各リポジトリの詳細],
  ),
  caption: [本書が扱う範囲],
) <tab-doc-scope>

#pagebreak()
